<!DOCTYPE html>
<html>
  <head>
    <title>Basecamp KPI Extractor</title>
  </head>
  <body>
    <h1>Welcome there! 👋🏼</h1>
    <dl>
      <dt>Basecamp Connection</dt>
      <% if accounts.any? %>
        <dd>
        Up and running 
          <% accounts.each do |account| %>
            <a href=<%= "/bc3-account/#{account.id}/projects" %>><%= account.name %></a>
          <% end %>
        </dd>
      <% else %>
        <dd><a href="/login">Connect</a></dd>
      <% end %>
      <dt>Github</dt>
      <dd>Not configured</dd>
    </dl>
  </body>

    <footer>
    Running BKE <%= BKE::VERSION %> 🚀
    </footer>
</html>
