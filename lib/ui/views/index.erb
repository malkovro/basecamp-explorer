<!DOCTYPE html>
<html>
  <head>
    <title>Basecamp KPI Extractor</title>
  </head>
  <body>
    <h1>Welcome there! 👋🏼</h1>
    <dl>
      <dt>Basecamp Connection</dt>
      <% if accounts.any? %>
        <dd>
        Up and running on
          <% accounts.each do |account| %>
            <a href=<%= "/bc3-account/#{account.id}/projects" %>><%= account.name %></a>
          <% end %>
        </dd>
      <% else %>
        <dd><a href="/basecamp/login">Connect</a></dd>
      <% end %>
      <dt>Github</dt>
       <% if gh_connected %>
        <dd>Connected 🤘</dd>
      <% else %>
        <dd><a href="/github/login">Connect</a></dd>
      <% end %>
    </dl>
  </body>

    <footer>
    Running BKE <%= BKE::VERSION %> 🚀
    </footer>
</html>
