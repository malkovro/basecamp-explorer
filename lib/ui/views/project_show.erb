<!DOCTYPE html>
<html>
  <head>
    <title>Basecamp KPI Extractor</title>
  </head>
  <body>
    <h1>Project <%= "#{project.name} (##{project.id})" %></h1>
    <h2>🕵🏽 Here you'll soon be able to see the KPIs 📈</h2>
    <p>
      <ul>
        <% todolists.each do |todolist| %>
          <li><a href=<%= "/bc3-account/#{account.id}/projects/#{project.id}/todolist/#{todolist.id}" %>><%= todolist.title %></a></li>
        <% end %>
      </ul>
      Archived ones:
      <ul>
        <% archived_todolists.each do |todolist| %>
          <li><a href=<%= "/bc3-account/#{account.id}/projects/#{project.id}/todolist/#{todolist.id}" %>><%= todolist.title %></a></li>
        <% end %>
      </ul>
    </p>
    <p>
      <a href=<%= project.app_url %>>See on Basecamp</a>
    </p>
  </body>
</html>
